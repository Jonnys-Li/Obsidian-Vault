今天的任务就是另起炉灶，重新理解UITableView，CollectionVIew，并自己尝试独立创建一份新的代码
## 今天的收获
- 学了什么-> UITableView应该如何使用，（创建DataSource，遵守DATASource协议，常见的代理方法，Cell的属性设置，用Model的方法去展示数据，与业务分离，如何通过plist转换Model，UITableView Controller是什么，如何在消息显示的过程中进行性能优化，如何使用注册Cell的形式进行性能优化，如何构建索引条）
- 尚未学到-> 自定义Cell的各种方式（frame，auto layout，xib/storyBoard，不等高的cell）
我们一项一项来：
### 11.1 设置UITableView的dataSource和delegate
1. UITableView继承于UIScrollerView，所以原生支持滚动屏幕。
2. 想要用UITableView，就必须遵守`UITableViewDataSouce`的协议
	1. 需要实现的DataSource：
		1. `-(NSInteger)numberOfSectionsInTableView:(UITableView *)tableView`-- 解决数据有几组的问题
		2. `-(NSInteger)tableView:(UITableView *)tbaleView numberOfRowsInSection:(NSInteger)section` --解决数据有几行的问题（可以不实现，不实现默认一行）
		3. `-(UITableViewCell *)tableView:(UITabelView *) tabelView cellForRowAtindexPath:(NSIndecPtah *)indexPath` --解决每一行里是什么东西的问题（里面的主要组件是什么）-》本质都是cell对象
3. ![[截屏2025-12-31 18.29.06.png]]
4. 选group样式才可以让格式分开
5. ![[截屏2025-12-31 18.29.49.png]]
6. 